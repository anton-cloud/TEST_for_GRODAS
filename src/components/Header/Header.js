import Container from "../Container";

const Header = () => {
    return (
        <header>
            <Container>
                <p style={{ textAlign: "center" }}>HEADER</p>
            </Container>
        </header>
    );
}

export default Header;